package mrkirby153.MscHouses;

public class StatueGen {

	/*
	 * public void zombie(int xPos, int yPos, int zPos, World world){ //Feet
	 * for(int y = 0; y < 2; y++){ for(int x = 0; x < 2; x++){ for(int z = 0; z
	 * < 6; z++){ world.setBlock(x+xPos, yPos+y, z+zPos, Block.stone.blockID);
	 * world.spawnParticle("hugeexplosion", x+xPos,yPos+y, zPos, 0, 0, 0); } } }
	 * //Pants for(int y = 0; y < 9; y++){ for(int x = 0; x < 2; x++){ for(int z
	 * = 0; z < 6; z++){ world.setBlock(x+xPos, y+yPos+2, z+zPos,
	 * Block.cloth.blockID); } } } for(int y = 0; y < 10; y++){ for(int x = 0; x
	 * < 2; x++){ for(int z = 0; z < 6; z++){ world.setBlock(x+xPos, y+yPos+11,
	 * z+zPos, Block.cloth.blockID); } } }
	 * 
	 * world.setBlock(xPos+1, yPos+20, zPos-1, Block.cloth.blockID); }
	 */
}
